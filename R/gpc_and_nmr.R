### turn into function


# convers <- db_nmr %>% nmr_kinetic(id = "hw15")
# mn <- db_gpc %>% filter(reaction_id == "hw15")
# full_join(mn, convers, "time")
#
# join %>% ggplot(aes(conversation, mn)) + geom_point() + geom_smooth(method = lm)
